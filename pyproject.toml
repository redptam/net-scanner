[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "net-scanner"
version = "1.0.0"
requires-python = ">=3.13"

[project.scripts]
netscanner = "net_scanner_gui.main:main"

[tool.setuptools.packages.find]
include = ["net_scanner_gui*"]
